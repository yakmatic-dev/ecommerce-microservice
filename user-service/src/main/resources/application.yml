spring:
  datasource:
    url: jdbc:postgresql://192.168.0.176:5432/user_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: user-service
server:
  port: 8082
