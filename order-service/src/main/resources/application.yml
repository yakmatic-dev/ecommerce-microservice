# Placeholder for application.yml
# File: order-service/src/main/resources/application.yml
spring:
  datasource:
    url: jdbc:postgresql://192.168.0.176:5432/order_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: order-service
server:
  port: 8083
